import 'package:flutter/material.dart';

class RepairGuidesScreen extends StatelessWidget {
  const RepairGuidesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Repair Guides')),
      body: const Center(child: Text('Repair Guides Screen')),
    );
  }
}
