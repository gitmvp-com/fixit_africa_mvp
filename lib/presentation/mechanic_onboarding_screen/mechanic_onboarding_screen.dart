import 'package:flutter/material.dart';

class MechanicOnboardingScreen extends StatelessWidget {
  const MechanicOnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mechanic Onboarding')),
      body: const Center(child: Text('Mechanic Onboarding Screen')),
    );
  }
}
