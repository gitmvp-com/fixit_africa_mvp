import 'package:flutter/material.dart';

class BankDetailsSetupScreen extends StatelessWidget {
  const BankDetailsSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bank Details Setup')),
      body: const Center(child: Text('Bank Details Setup Screen')),
    );
  }
}
