import 'package:flutter/material.dart';

class BulkUpdateScreen extends StatelessWidget {
  const BulkUpdateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bulk Update')),
      body: const Center(child: Text('Bulk Update Screen')),
    );
  }
}
