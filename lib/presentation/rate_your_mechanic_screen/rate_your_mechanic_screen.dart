import 'package:flutter/material.dart';

class RateYourMechanicScreen extends StatelessWidget {
  const RateYourMechanicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rate Mechanic')),
      body: const Center(child: Text('Rate Your Mechanic Screen')),
    );
  }
}
