import 'package:flutter/material.dart';

class TopMechanicsGaragesScreen extends StatelessWidget {
  const TopMechanicsGaragesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Top Mechanics & Garages')),
      body: const Center(child: Text('Top Mechanics & Garages Screen')),
    );
  }
}
