import 'package:flutter/material.dart';

class RateGarageScreen extends StatelessWidget {
  const RateGarageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rate Garage')),
      body: const Center(child: Text('Rate Garage Screen')),
    );
  }
}
