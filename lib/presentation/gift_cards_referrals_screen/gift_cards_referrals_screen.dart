import 'package:flutter/material.dart';

class GiftCardsReferralsScreen extends StatelessWidget {
  const GiftCardsReferralsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gift Cards & Referrals')),
      body: const Center(child: Text('Gift Cards & Referrals Screen')),
    );
  }
}
