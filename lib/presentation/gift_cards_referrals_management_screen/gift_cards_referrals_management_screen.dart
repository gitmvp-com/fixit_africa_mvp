import 'package:flutter/material.dart';

class GiftCardsReferralsManagementScreen extends StatelessWidget {
  const GiftCardsReferralsManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gift Cards & Referrals Management')),
      body: const Center(child: Text('Gift Cards & Referrals Management Screen')),
    );
  }
}
