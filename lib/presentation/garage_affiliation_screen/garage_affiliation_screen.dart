import 'package:flutter/material.dart';

class GarageAffiliationScreen extends StatelessWidget {
  const GarageAffiliationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Garage Affiliation')),
      body: const Center(child: Text('Garage Affiliation Screen')),
    );
  }
}
