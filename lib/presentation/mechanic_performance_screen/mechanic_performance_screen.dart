import 'package:flutter/material.dart';

class MechanicPerformanceScreen extends StatelessWidget {
  const MechanicPerformanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mechanic Performance')),
      body: const Center(child: Text('Mechanic Performance Screen')),
    );
  }
}
